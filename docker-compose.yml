services:
  app:
    build: .
    container_name: actividadapp
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
    volumes:
      - evidencias:/app/wwwroot/evidencias
    restart: unless-stopped

volumes:
  evidencias:
